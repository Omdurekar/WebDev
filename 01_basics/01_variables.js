const accountId = 123455; // cannot be changed , using semicolon is optional
let accountName = 'om durekar' // prefer this over var coz of block scope , we can use double quotes or single quotes for string values, using semicolon is optional
var accountBalance = 1000 // can be changed and has function scope
accountCity = "pune" //variable without declaration, not recommended

console.log(accountId) //we can print the value of accountId
console.log([accountName, accountBalance, accountCity]) //we can print the values of accountName, accountBalance and accountCity in a tabular format

